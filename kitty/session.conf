# Default startup session for Kitty
# This creates an initial layout similar to your tmux setup

# Create initial tab with session name
new_tab main
focus

# You can add more tabs here if needed
# new_tab dev
# new_tab logs

# Example: Create a split layout in the main tab
# launch --location=vsplit --cwd=~
# launch --location=hsplit --cwd=~/Projects
